# 📉 Technical Debt & Roadmap

Этот документ отслеживает известные технические компромиссы и планы по развитию архитектуры проекта.

## 🔴 Высокий приоритет (Architecture)

### 1. Политика "Zero HTML Tags" в слое Tools

Слой `view/tools/` должен содержать только бизнес-логику и композицию компонентов из `/ui`. Наличие базовых тегов (например, `<div>`, `<span>`, `section`) в этом слое считается "рудой", которую нужно очистить.

- **План:** Постепенная замена всех `div` на `Box`, `Stack`, `Group` или специализированные компоненты.
- **Linter:** Рассмотреть внедрение правила `react/forbid-dom-elements` для папки `view/tools/`.

### 2. Стратегия Слияния: Lib vs Tool Subsystems

После внедрения паттерна **Domain Hooks** (React в `lib/`), граница между "Библиотекой" (`lib/graphics`) и "Подсистемой Инструмента" (`view/tools/_video`) стала размытой.

- **Симптом:** `useImageMetadata` живет в `lib`, а `useFrameExtractor` — в `view`. Оба являются сложной логикой управления состоянием.
- **Риск:** Фрагментация логики. Разработчик не знает, где искать/создавать хук: в глобальном домене или в локальной папке инструмента.
- **Задача:** Определить строгий критерий или слить понятия.
  - _Вариант А (Consolidation):_ Превратить все `_subsystems` в модули `lib/`. `view` остается только для компоновки.
  - _Вариант Б (Promotion Protocol):_ Оставить код в `view` (Mining phase), и переносить в `lib` (Refining phase) только при переиспользовании в **двух и более** инструментах.

### 3. Zero-Copy Transfer в Web Workers

Текущая реализация `background-engine.worker.ts` передает `imageData` через стандартный `postMessage`.

- **Проблема:** Для изображений 4K+ происходит клонирование больших массивов, что вызывает микро-фризы UI и удваивает потребление памяти.
- **План:** Внедрить использование **Transferable Objects** (передача владения `ArrayBuffer` без копирования) для всех операций с пикселями.

## 🟡 Средний приоритет (Reliability & DX)

### 1. Внедрение State Machines (Расширение)

Внедрен хук `useTask.ts` для управления асинхронными процессами.

- **Status (25-12-2025):** `VideoFrameExtractor` переведен на `useTask`.
- **Target:** `MonochromeBackgroundRemover`. В нём есть много флагов (`isProcessing`, `manualTrigger`, `floodPoints`), которые нужно заменить на строгий FSM.

### 2. Integration Testing (E2E)

- **Status:** Логика ядра хорошо покрыта Unit-тестами (`vitest`), но UI-сценарии не тестируются.
- **Риск:** Регрессии в пользовательских сценариях (Drag & Drop -\> Обработка -\> Скачивание).
- **План:** Добавить Playwright или Cypress для smoke-тестирования критических путей основных инструментов.

### 3. Web Worker Optimization (Gifshot)

- **Status:** Логика `gifshot` изолирована в `lib/modules/video/gif-encoder.ts`, но выполняется в основном потоке.
- **План:** Перенести `gif-encoder.ts` внутрь Web Worker для предотвращения блокировки UI при генерации гифок.

## 🟢 Низкий приоритет

### 1. Выделение Доменных Примитивов

Необходимо стандартизировать работу с Canvas и Video во всех инструментах.

- Выделить `viewport` (зум, панорамирование, координаты).
- Выделить `playback` (управление временем видео, синхронизация).

### 2. Strict TS vs Next.js Conflict

Игнорирование `.next/types` из-за несоответствия строгости (`noUncheckedIndexedAccess`). Решение отложено до стабилизации типов в Next.js App Router.

### 3. Canvas Logic Separation

Компонент `Canvas.tsx` перегружен ответственностью.

- **План:** Выделить математику зума и панорамирования в чистый хук `useViewportLogic` или класс.

### 4. Конфигурация Приватности в Линтере

Текущая реализация правил изоляции в `eslint-plugin-boundaries` (для папок с префиксом `_`) работает не совсем корректно из-за конфликта режимов определения элементов.

- **Проблема:** Смешивание `mode: file` и `mode: folder` усложняет надежное определение "внутренностей" модуля для правил `entry-point`.
- **План:** Вернуться к этому вопросу позже для унификации определения границ модулей и корректной настройки `allow/disallow` для приватных путей.
