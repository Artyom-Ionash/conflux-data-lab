# 📉 Technical Debt & Roadmap

Этот документ отслеживает известные технические компромиссы и планы по развитию архитектуры проекта.

## 🔴 Высокий приоритет (Architecture)

### 1. Исправление Layout Инструментов (Workbench v2)

**Проблема:**
Текущая реализация `Workbench` страдает от наследия "веб-сайтовой" верстки.

1.  **Container Trap:** Инструменты обернуты в `container mx-auto px-4`, создавая паразитные отступы по бокам и сверху. Инженер требует использования 100% полезной площади экрана (Industrial UI).
2.  **Scroll Leak:** Скроллинг внутри Canvas или Sidebar вызывает прокрутку всей страницы (body scroll), что недопустимо для Desktop-like приложений.
3.  **Layout Shift:** Хедер и навигация "съедают" место и не интегрированы в экосистему инструмента.

**План действий:**

- [ ] **Full-Viewport Mode:** Убрать обертки `container` из `app/tools/**/page.tsx`. `Workbench` должен быть корневым элементом `page`.
- [ ] **Scroll Isolation:** Применить `overscroll-behavior: none` и фиксированную высоту `100dvh` для предотвращения прокрутки `body`.
- [ ] **Adaptive Header:** Сделать хедер частью `Workbench` или скрывать его при работе в инструменте для максимизации рабочей области.

### 2. Декомпозиция на доменные примитивы

Выделение `lib/modules/viewport` и `lib/modules/playback`.

## 🟡 Средний приоритет (DX & Stability)

- **Web Worker optimization:** Вынос генерации GIF в воркер.
- **Декомпозиция "Монолитов"** Хотя файлы перенесены в `view/instruments`, некоторые из них (например, `VideoFrameExtractor.tsx`) все еще слишком велики.

## 🟢 Низкий приоритет

- Strict TS vs Next.js conflict (types generation).
