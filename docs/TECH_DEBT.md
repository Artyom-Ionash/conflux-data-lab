# 📉 Technical Debt & Roadmap

Этот документ отслеживает известные технические компромиссы и планы по развитию проекта.

## 🟡 Средний приоритет (DX & Stability)

### 1. Систематизация слоев вёрстки (Layout & Z-Index)

**Проблема:** В компонентах (`Canvas`, `ToolLayout`, `Modal`) встречаются хардкодные значения `z-index`. Это создает риск "войн слоев".
**План действий:**

- [ ] Провести аудит текущих `z-index`.
- [ ] Внедрить семантическую систему слоев в тему Tailwind (`z-modal`, `z-tooltip`).

### 2. Рефакторинг обработки данных в Pipeline-стиль

**Проблема:** В модулях обработки изображений (`filters.ts`, `processor.worker.ts`) используется императивный стиль с промежуточными переменными. Это усложняет понимание потока данных и композицию операций.

**Примеры текущего подхода:**

```typescript
// Текущий стиль - императивный
let alphaChannel = applyColorFilter(...);
if (settings.edgeChoke > 0) {
  alphaChannel = applyMorphologyChoke(alphaChannel, ...);
}
if (settings.edgeBlur > 0) {
  alphaChannel = applyBlur(alphaChannel, ...);
}
```

**Целевой подход - конвейерный (pipeline):**

```typescript
const alphaChannel = pipe(
  data,
  applyColorFilter(settings),
  when(settings.edgeChoke > 0, applyMorphologyChoke(settings.edgeChoke)),
  when(settings.edgeBlur > 0, applyBlur(settings.edgeBlur))
);
```

**Преимущества:**

- Декларативность и читаемость потока трансформаций
- Простота добавления/удаления шагов обработки
- Возможность переиспользования композиций
- Легкость unit-тестирования отдельных этапов

**План действий:**

- [ ] Исследовать легковесные библиотеки для pipe/compose (или реализовать собственные утилиты)
- [ ] Рефакторинг `lib/modules/graphics/processing/` в функциональный стиль
- [ ] Внедрить паттерн "Conditional Pipeline" для опциональных шагов
- [ ] Обновить документацию с примерами композиции операций

## 🟢 Низкий приоритет (Optimizations & Features)

### 1. Конфликт Strict TS и Next.js

**Проблема:** Флаг `noUncheckedIndexedAccess: true` ломает автогенерацию типов Next.js (`.next/dev`).
**Компромисс:** Папка `.next/dev` исключена из проверки типов в `tsconfig.json`.
**План действий:**

- [ ] Мониторить обновления Next.js на предмет поддержки строгих флагов.

### 2. Дублирование конфигурации путей (Vitest)

**Проблема:** Алиас `@/` настроен явно в `vitest.config.mts`, дублируя настройки `tsconfig.json`.
**План действий:**

- [ ] При смене алиасов не забыть обновить оба конфига.

### 3. Валидация имен классов Tailwind

**Проблема:** Нет проверки существования классов Tailwind v4.
**План действий:**

- [ ] Дождаться выхода плагина `eslint-plugin-tailwindcss` для v4.

### 4. Web Workers и Параллелизм в Браузере

**Проблема:** Тяжелые операции блокируют UI. Текущая реализация использует один Web Worker для обработки изображений.

**Текущее состояние:**

- ✅ Worker для графических фильтров (`processor.worker.ts`)
- ❌ GIF-генерация (`gifshot`) блокирует главный поток
- ❌ Отсутствие параллельной обработки массивов кадров

**Перспективные направления:**

**A. Worker Pool для пакетной обработки**

```typescript
// Распараллеливание обработки 100 кадров на 4 воркера
const pool = new WorkerPool(4, createProcessorWorker);
const results = await pool.processAll(frames, processFrame);
```

**B. SharedArrayBuffer + Atomics (Experimental)**

- Sharing memory между воркерами для zero-copy операций
- Требует специальных HTTP заголовков (`Cross-Origin-Isolated`)

**C. OffscreenCanvas**

- Рендеринг Canvas в Web Worker (поддержка: Chrome 69+, Firefox 105+)
- Идеально для генерации спрайтов и preview

**D. WebAssembly (WASM) + SIMD**

- Компиляция критичных алгоритмов (Morphology, Blur) в WASM
- SIMD инструкции для параллельной обработки пикселей (4-16x ускорение)

**План действий:**

- [ ] Перенести генерацию GIF (`gifshot`) в Web Worker
- [ ] Реализовать Worker Pool для пакетной обработки кадров
- [ ] Исследовать `OffscreenCanvas` для фоновой генерации превью
- [ ] Proof-of-Concept: WASM модуль для морфологических операций
- [ ] Benchmark: сравнение Native JS vs WASM vs WASM+SIMD

### 5. Рефакторинг `lib/core/utils`

**Проблема:** Смешанная ответственность в `utils`.
**План действий:**

- [ ] Разделить на `math`, `browser`, `data` при росте количества файлов.
