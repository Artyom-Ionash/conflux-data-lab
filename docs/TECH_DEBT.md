# 📉 Technical Debt & Roadmap

Этот документ отслеживает известные технические компромиссы и планы по развитию архитектуры проекта.

## 🔴 Высокий приоритет (Architecture)

### 1. Декомпозиция "Монолитов" (Refactoring)

**Проблема:**
Некоторые инструменты выросли в "Монолиты", смешивающие UI, стейт-менеджмент и бизнес-логику. Это усложняет поддержку и тестирование.

- **Target:** `view/tools/VideoFrameExtractor.tsx` (~470 строк).
- **План:**
  1.  Выделить логику состояния (хук `useVideoFrameExtraction`) в отдельный файл `lib/modules/video/use-extraction.ts`.
  2.  Вынести UI-блоки (Slider, Preview Grid) в подпапку `view/tools/video/components/`.
  3.  Изолировать логику `gifshot` в `lib/modules/video/encoders`.

### 2. Выделение Доменных Примитивов

Необходимо стандартизировать работу с Canvas и Video во всех инструментах.

- Выделить `lib/modules/viewport` (зум, панорамирование, координаты).
- Выделить `lib/modules/playback` (управление временем видео, синхронизация).

## 🟡 Средний приоритет (Performance & DX)

### 1. Web Worker Optimization

Хотя `useWorker` существует, тяжелые операции генерации GIF (в `VideoFrameExtractor`) и некоторые фильтры всё ещё могут блокировать Main Thread.

- **План:** Перенести `gifshot` и тяжелую математику цвета полностью в воркеры.

## 🟢 Низкий приоритет

- **Strict TS vs Next.js Conflict:** Игнорирование `.next/types` из-за несоответствия строгости (`noUncheckedIndexedAccess`). Решение отложено до стабилизации типов в Next.js App Router.
