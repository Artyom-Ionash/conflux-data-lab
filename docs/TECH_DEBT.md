# 📉 Technical Debt & Roadmap

Этот документ отслеживает известные технические компромиссы и планы по развитию архитектуры проекта.

## 🟠 Bugs & Regressions

### 1. VideoFrameExtractor: Артефакт прозрачного кадра

**Проблема:**
Иногда при извлечении кадров первый кадр (t=0) оказывается абсолютно прозрачным (пустым), даже если видео не имеет альфа-канала.
**Вероятная причина:**
Race condition при инициализации видео-элемента. `drawImage` на Canvas вызывается после события `seeked`, но браузер (особенно Chrome) может не успеть декодировать первый кадр в буфер отрисовки.
**План исправления:**

- [ ] Добавить проверку `video.readyState >= 2` перед отрисовкой.
- [ ] Внедрить "warm-up" отрисовку или двойной `requestAnimationFrame` перед захватом кадра.

## 🔴 Высокий приоритет (Architecture & Decoupling)

### 1. Стандартизация стилизации (Tailwind Management)

**Статус:** 🔄 В процессе внедрения.
**Прогресс:**

- [x] Создана утилита `cn`.
- [x] Отрефакторены сложные компоненты (`VerticalImageAligner`, `TextureDimensionSlider`) под новые стандарты.
- [ ] **Global Sweep:** Пройтись по остальным UI-компонентам и вычистить шаблонные строки.

### 2. Декомпозиция на доменные примитивы (Domain-Driven Decomposition)

**Проблема:** Логика инструментов ("Page Controllers") сильно связана с UI.
**План действий:**

- [ ] **Viewport & Interaction:** Выделить логику Pan/Zoom в `lib/modules/viewport`.
- [ ] **Media Playback:** Выделить управление видео и кадрами в `lib/modules/playback`.
- [ ] **Worker Orchestration:** Создать обобщенный `JobScheduler`.
- [ ] **Undo/Redo:** Реализовать `useHistory<T>`.

### 3. Реализация Pipeline-обработки

**Проблема:** Императивный код в воркерах.
**План:**

- [ ] Создать утилиту `pipe` в `lib/core/utils/pipeline.ts`.
- [ ] Отрефакторить `processor.worker.ts`.

## 🟡 Средний приоритет (DX & Stability)

### 1. Систематизация слоев вёрстки (Z-Index)

**Проблема:** Хардкодные значения `z-index`.
**План:** Внедрить семантическую систему слоев в Tailwind (`z-modal`, `z-tooltip`).

### 2. Оптимизация Web Workers

**Проблема:** Блокировка UI при генерации GIF.
**План:** Перенести `gifshot` в Web Worker и реализовать пул воркеров.

## 🟢 Низкий приоритет (Optimizations)

### 1. Конфликт Strict TS и Next.js

Папка `.next/dev` исключена из проверки типов.

### 2. Валидация Tailwind v4

Ожидание официального плагина ESLint для v4.
