# 📉 Technical Debt & Roadmap

Этот документ отслеживает известные технические компромиссы в коде проекта.

## 🟠 Bugs & Regressions

### 1. VideoFrameExtractor: Артефакт прозрачного кадра

**Проблема:** Первый кадр иногда прозрачный.
**План:** Добавить `video.readyState` чек и warm-up рендеринг.

## 🔴 Высокий приоритет (Architecture)

### 1. Global Sweep: Tailwind Standardization

**Задача:** Пройтись по всем UI-компонентам и заменить шаблонные строки на `cn()`.

### 2. Декомпозиция на доменные примитивы

Выделение `lib/modules/viewport` и `lib/modules/playback`.

### 3. Pipeline Processing

Рефакторинг `processor.worker.ts` на использование `pipe`.

## 🟡 Средний приоритет (DX & Stability)

- Z-Index system.
- Web Worker optimization (Gifshot).

## 🟢 Низкий приоритет

- Strict TS vs Next.js conflict.
