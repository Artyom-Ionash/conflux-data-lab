# 📉 Technical Debt & Roadmap

Этот документ отслеживает известные технические компромиссы и планы по развитию архитектуры проекта.

## 🔴 Высокий приоритет (Architecture & Decoupling)

### 1. Разделение UI и Бизнес-логики (Logic Extraction)

**Проблема:** В некоторых инструментах (например, `MonochromeBackgroundRemover.tsx`, `VerticalImageAligner.tsx`) логика управления состоянием, оркестрация воркеров и UI-рендеринг смешаны в одном файле. Это делает компоненты "толстыми", сложными для тестирования и жестко привязанными к React.

**Целевая архитектура:**

- **UI (View):** "Глупые" компоненты, которые только отображают данные и дергают колбэки.
- **Logic (ViewModel/Controller):** Кастомные хуки (или ванильные классы), управляющие состоянием, валидацией и сайд-эффектами.
- **Core (Model):** Чистые функции и алгоритмы (уже реализовано в `lib/`).

**План действий:**

- [ ] **Phase 1 (Hooks):** Рефакторинг `MonochromeBackgroundRemover`. Вынос логики в хук `useMonochromeLogic` (по аналогии с `useVideoFrameExtraction`).
- [ ] **Phase 2 (Agnostic):** Исследование возможности выноса стейта в Framework-agnostic сторы (например, `zustand/vanilla` или классы), чтобы логику можно было тестировать без рендеринга React-компонентов.
- [ ] Унификация интерфейсов взаимодействия с Web Workers внутри этих хуков/сторов.

### 2. Реализация Pipeline-обработки (Functional Composition)

**Проблема:** Императивный стиль вызова фильтров в воркерах затрудняет чтение и расширение.
**Решение:** Использовать паттерн "Pipe" для композиции функций обработки.

**План действий:**

- [ ] Создать легковесную утилиту `pipe` и `when` в `lib/core/utils/pipeline.ts` (Native implementation, без внешних библиотек).
- [ ] Отрефакторить `processor.worker.ts` с использованием pipeline-подхода.
- [ ] Применить этот паттерн для новых инструментов обработки данных.

## 🟡 Средний приоритет (DX & Stability)

### 1. Систематизация слоев вёрстки (Layout & Z-Index)

**Проблема:** В компонентах (`Canvas`, `ToolLayout`, `Modal`) встречаются хардкодные значения `z-index`. Это создает риск "войн слоев".
**План действий:**

- [ ] Провести аудит текущих `z-index`.
- [ ] Внедрить семантическую систему слоев в тему Tailwind (`z-modal`, `z-tooltip`).

### 2. Оптимизация Web Workers

**Проблема:** Тяжелые операции блокируют UI или выполняются неэффективно.
**План действий:**

- [ ] Перенести генерацию GIF (`gifshot`) в Web Worker (сейчас блокирует UI).
- [ ] Реализовать Worker Pool для пакетной обработки кадров (параллелизм).

## 🟢 Низкий приоритет (Optimizations & Features)

### 1. Конфликт Strict TS и Next.js

**Проблема:** Флаг `noUncheckedIndexedAccess: true` ломает автогенерацию типов Next.js (`.next/dev`).
**Компромисс:** Папка `.next/dev` исключена из проверки типов в `tsconfig.json`.

### 2. Дублирование конфигурации путей (Vitest)

**Проблема:** Алиас `@/` настроен явно в `vitest.config.mts`, дублируя настройки `tsconfig.json`.

### 3. Валидация имен классов Tailwind

**Проблема:** Нет проверки существования классов Tailwind v4.
**План действий:** Дождаться выхода официального плагина ESLint для v4.
