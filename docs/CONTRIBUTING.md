# Руководство для разработчиков (Contributing Guide)

## 🚀 Быстрый старт

1.  **Установка зависимостей:**

    ```bash
    npm install
    npm run prepare # Установка git-хуков
    ```

2.  **Запуск в режиме разработки:**
    ```bash
    npm run dev
    ```
    Запускает параллельно:
    - Next.js сервер (`localhost:3000`)
    - TypeScript checker (watch mode)
    - Генератор контекста для AI (`.context/project.txt`)

## 📐 Стиль кода и Линтинг

Мы используем **ESLint** (качество) и **Prettier** (форматирование).

- **Strict TypeScript:** Не используйте `any` или `ts-ignore` без веской причины.
- **Imports:** Сортировка импортов автоматическая (`simple-import-sort`).

### Правила комментирования

1.  **Язык:** Комментарии на **русском** (для скорости восприятия), код и переменные — строго на **английском**.
2.  **Синтаксис:** Обязательный пробел после слешей: `// Комментарий`.
3.  **TSDoc:** Публичные утилиты в `lib/core` должны иметь описание в формате `/** ... */`.
4.  **Смысл:** Комментируйте **"Почему"** (причина решения), а не "Что" (дублирование кода).

## 🧪 Тестирование

Используется **Vitest**.

- `npm test` — Запуск всех тестов.
- `npm run test:watch` — Запуск в режиме наблюдения.

> **Pre-commit:** При коммите автоматически запускаются тесты только для измененных файлов.

## 🦾 Работа с ИИ

В проекте генерируется файл `.context/project.txt`. Используйте его содержимое для загрузки контекста проекта в LLM (ChatGPT, Claude), чтобы нейросеть понимала структуру и архитектуру проекта.

## 🏗 Создание инструментов

Подробная инструкция по архитектуре инструментов находится в `docs/ARCHITECTURE.md`.
