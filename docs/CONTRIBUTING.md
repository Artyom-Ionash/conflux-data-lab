# Руководство для разработчиков (Contributing Guide)

## 🚀 Быстрый старт

1.  **Установка зависимостей:**

    ```bash
    npm install
    ```

2.  **Запуск в режиме разработки:**
    ```bash
    npm run dev
    ```
    Запускает параллельно:
    - Next.js сервер (`localhost:3000`)
    - TypeScript checker (watch mode)
    - Генератор контекста для AI (`.context/project.txt`)

## 📐 Стиль кода и Линтинг

Мы используем **ESLint** (качество) и **Prettier** (форматирование).

- **Strict TypeScript:** Используйте строгую типизацию для обеспечения надежности кода.
- **Imports:** Сортировка импортов происходит автоматически при сохранении (`simple-import-sort`).

### Правила комментирования

1.  **Язык:** Комментарии пишите на **русском** (для скорости восприятия), код и переменные — строго на **английском**.
2.  **TSDoc:** Добавляйте описание в формате `/** ... */` для публичных утилит в `lib/core`.
3.  **Смысл:** Описывайте **"Почему"** (причина архитектурного решения), а не "Что" (дублирование логики кода текстом).

## ⚡ Стратегии эффективного кода (Code Efficiency)

Для поддержания чистоты и компактности кодовой базы придерживайтесь следующих практик:

1.  **Abstraction over Extraction:**
    При рефакторинге стремитесь создавать универсальные абстракции (например, `useImageProcessor` или `useWorkerTask`), которые можно переиспользовать в разных инструментах. Это эффективнее, чем простое вынесение узкоспециализированной логики в отдельный файл, что часто лишь увеличивает объем кода.

2.  **Colocation & YAGNI:**
    Храните типы, константы, стили и вспомогательные функции в одном файле с компонентом. Выносите их в общие модули только тогда, когда они действительно потребуются в другом месте приложения.

3.  **Inference:**
    Активно используйте возможности TypeScript по выводу типов (например, `typeof`, `ReturnType`) для описания пропсов и стейта. Это позволяет избежать ручного дублирования интерфейсов и уменьшает количество шаблонного кода.

## 🧪 Тестирование

Используется **Vitest**.

- `npm test` — Запуск всех тестов.
- `npm run test:watch` — Запуск в режиме наблюдения.

> **Pre-commit:** При коммите автоматически запускаются тесты только для измененных файлов.

## 🧠 Работа с ИИ

В проекте генерируется файл `.context/project.txt`. Используйте его содержимое для загрузки контекста проекта в LLM (**Gemini 3 Pro**, **DeepSeek**), чтобы нейросеть понимала структуру и архитектуру проекта.

## 🏗 Создание инструментов

Подробная инструкция по архитектуре инструментов находится в `docs/ARCHITECTURE.md`.
