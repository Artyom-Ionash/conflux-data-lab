# Руководство для разработчиков (Contributing Guide)

## 🚀 Быстрый старт

1.  **Установка зависимостей:**

    ```bash
    npm install
    ```

2.  **Запуск в режиме разработки:**
    ```bash
    npm run dev
    ```
    Запускает параллельно:
    - Next.js сервер (`localhost:3000`)
    - TypeScript checker (watch mode)
    - Генератор контекста для AI (`.context/project.txt`)

## 📐 Стиль кода и Линтинг

Мы используем **ESLint** (качество) и **Prettier** (форматирование).

- **Strict TypeScript:** Используйте строгую типизацию. Проект настроен на максимальную строгость (`noUncheckedIndexedAccess`).
- **Imports:** Сортировка импортов происходит автоматически при сохранении.

### Правила Именования (Naming Convention)

Мы следуем строгой семантике файловой системы:

1.  **Папки (Directories):**
    - Строго **kebab-case**: `ui/input`, `lib/file-system`.
    - **\_underscore-prefix**: Для структурных/приватных папок (`app/_ui`, `features/_video`, `lib/context-generator/_assembly.ts`).

2.  **Файлы (Files):**
    - **PascalCase**: Только для React-компонентов и классов (`Button.tsx`, `ToolCard.tsx`).
    - **kebab-case**: Для хуков, утилит, конфигов и страниц Next.js (`use-worker.ts`, `page.tsx`, `layout.tsx`).

### Правила комментирования

1.  **Язык:** Комментарии — на **русском**, код — на **английском**.
2.  **TSDoc:** Обязателен для переиспользуемых утилит в `lib/core`.
3.  **Принцип "Зачем, а не Что":** Объясняйте причины неочевидных решений, а не дублируйте код словами.

## ♻️ Политика Deprecation (Устаревания)

В нашей методологии ("Mining & Refining") код может устаревать быстро.

1.  **Запрет на "Вечное Deprecated":**
    Тег `@deprecated` используется только как краткосрочный маркер во время рефакторинга.
    - Если компонент используется только внутри проекта (не публичная библиотека), устаревший код должен быть удалён (а вызовы обновлены) **в том же Pull Request / коммите**.

2.  **Формат метки:**
    Если удаление невозможно сразу (например, сложная миграция), используйте формат:
    ```typescript
    /**
     * @deprecated [Причина]. Используйте [НовоеРешение].
     */
    ```

### 🎨 Управление стилями (CSS & Tailwind)

Для предотвращения конфликтов классов:

1.  **Утилита `cn()`:** Всегда используйте `cn()` для условных классов.
2.  **Варианты:** Используйте `cva` для компонентов с множеством состояний.
3.  **Кастомные утилиты (Custom Utilities):**
    Если вы видите часто повторяющийся паттерн (более 3 раз), вынесите его в `core/tailwind/utilities.css`.
    - **Префикс:** Строго **`fx-`** (Functional Extension).
      - ✅ `fx-center` (flex items-center justify-center)
      - ✅ `fx-cover` (absolute inset-0 h-full w-full)

4.  **Топология Z-Index (Слои):**
    Используйте семантические переменные из `core/tailwind/layers.css`:
    - **Контент:** `z-base` (0), `z-content` (1).
    - **Canvas:** `z-canvas-bg`, `z-canvas-content`, `z-canvas-ui` (HUD).
    - **UI Shell:** `z-sticky` (100), `z-workbench-sidebar` (200).
    - **Overlays:** `z-overlay` (500), `z-modal` (600), `z-tooltip` (1000).

## ⚡ Практики кодирования

Для поддержания чистоты кодовой базы:

1.  **YAGNI (You Aren't Gonna Need It):** Реализуйте только то, что необходимо.
2.  **Inference (Вывод типов):** Активно используйте возможности TypeScript.

_Фундаментальные принципы проектирования (Abstraction over Extraction, Colocation) вынесены в `ARCHITECTURE.md`._

### 🧩 Регистрация нового инструмента

При добавлении нового функционала:

1. Создайте файл в `features/` (например, `features/MyNewTool.tsx`).
2. Добавьте описание в `app/registry/inventory.ts`.
3. Добавьте динамический импорт в `app/registry/tool-loader.tsx`.

## 🧠 Работа с ИИ и Контекстом

Проект поддерживает локальную кастомизацию контекста через папку `.ai/`.

## 🏗 Архитектура

Подробная инструкция по структуре проекта находится в `docs/ARCHITECTURE.md`.
