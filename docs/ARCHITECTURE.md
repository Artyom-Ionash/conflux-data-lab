# Архитектура проекта

Проект построен на **Next.js 16 (App Router)** с использованием модульной архитектуры. Мы придерживаемся принципов, близких к **FSD (Feature-Sliced Design)**, но адаптированных для простоты и специфики приложения-портфолио.

## 🏗 Принципы слоев (Boundaries)

Мы используем жесткие правила линтера (`eslint-plugin-boundaries`), чтобы предотвратить запутывание зависимостей.

### 1. UI Layer (`app/components/primitives`)

- **Назначение:** Базовые "глупые" (presentational) компоненты.
- **Примеры:** `Button`, `Slider`, `Card`, `Modal`, `Canvas`.
- **Правила:**
  - Никакой бизнес-логики.
  - **Запрещено:** Импортировать что-либо из слоев `domain`, `tools`, `entities` или `app`.

### 2. Entity Layer (`app/components/entities`)

- **Назначение:** UI-компоненты, привязанные к конкретным бизнес-сущностям.
- **Примеры:** `TextureLimitIndicator` (знает про лимиты GPU), `RangeVideoPlayer` (знает про видео).
- **Правила:**
  - Может использовать `primitives` и `lib/modules`.
  - **Запрещено:** Зависеть от конкретных инструментов (`tools`).

### 3. Feature / Tool Layer (`app/components/tools`)

- **Назначение:** Полноценные виджеты инструментов. Это "сердце" приложения. Каждый инструмент собирает компоненты из `primitives`, `entities` и логику из `lib` в готовое решение.
- **Примеры:** `VideoFrameExtractor`, `JsonToCsvConverter`.
- **Правила:**
  - Инструменты должны быть изолированы друг от друга.
  - Один инструмент не должен знать о существовании другого.

### 4. Domain Logic & Core (`lib/`)

Этот слой разделен на два уровня абстракции:

#### A. Core (`lib/core`)

- **Назначение:** Фундаментальные строительные блоки приложения. Весь код здесь должен быть **агностиком** по отношению к бизнес-логике.
- **Критерий проверки:** _«Могу ли я скопировать эту папку в другой проект без изменений и ошибок?»_ Если да — этому место в Core.
- **Состав:**
  - `hooks/` — Общие React-хуки (`useObjectUrl`, `useDebounceEffect`).
    > _Принцип:_ Мы предпочитаем реализовывать простые утилитарные хуки самостоятельно (In-House), вместо подключения тяжелых библиотек типа `ahooks` или `react-use`, чтобы контролировать типизацию и зависимости.
  - `utils/` — Алгоритмические и платформенные хелперы.
    - `colors.ts`: Математика цвета, конвертация (Pure Functions).
    - `media.ts`: Обертки над Browser API (Canvas, DOM, Blob).
      > _Правило:_ Здесь **запрещено** импортировать типы из `modules` или `components`. Это "глупые" функции.

#### B. Modules (`lib/modules`)

- **Назначение:** Основная бизнес-логика, сгруппированная по доменам. Этот код знает о специфике приложения.
- **Состав:**
  - `graphics/` — Логика графического редактора (стандарты GPU, воркеры, пайплайны обработки).
  - `file-system/` — Парсеры специфичных форматов (Godot Scene), генераторы деревьев.
  - `tool-registry/` — Реестр инструментов, конфигурация метаданных.

---

## 🛡 Качество кода и TypeScript

Проект использует стратегию **Maximum Strictness**.

### Paranoia Mode

В `tsconfig.json` включены флаги, которые делают TypeScript максимально строгим, в частности:

- `noUncheckedIndexedAccess: true` — Заставляет проверять на `undefined` любой доступ к массиву по индексу (`arr[0]`).

### Конфликт с Next.js

Сгенерированные типы Next.js для Dev-режима (`.next/dev/types`) часто не соответствуют этой строгости.
**Решение:** Папка `.next/dev` добавлена в `exclude` в `tsconfig.json`. Мы проверяем только свой код, но не временные файлы фреймворка.

---

## 🧩 Как добавить новый инструмент

Процесс добавления инструмента стандартизирован и состоит из 3 шагов.

### Шаг 1. Конфигурация

Добавьте запись в файл `lib/modules/tool-registry/config.ts`:

```typescript
{
  id: 'my-new-tool',
  name: 'My New Tool',
  category: 'analysis',
  tags: ['new', 'beta'],
  description: 'Краткое описание инструмента.'
}
```

### Шаг 2. Реализация компонента

Создайте папку `app/components/tools/my-new-tool/` и файл `MyNewTool.tsx`:

```tsx
'use client';

import { ToolLayout } from '../ToolLayout';

export function MyNewTool() {
  const sidebar = <div>{/* Элементы управления */}</div>;

  return (
    <ToolLayout title="Мой инструмент" sidebar={sidebar}>
      {/* Основная рабочая область (Canvas, Editor и т.д.) */}
      <div className="p-4">Hello World</div>
    </ToolLayout>
  );
}
```

### Шаг 3. Регистрация (Dynamic Import)

Подключите компонент в реестр `lib/modules/tool-registry/loader.tsx`. Мы используем `next/dynamic` для code-splitting, чтобы код инструмента не загружался, пока пользователь не откроет его.

```typescript
const MyNewTool = dynamic(
  () => import('@/app/components/tools/my-new-tool/MyNewTool').then((mod) => mod.MyNewTool),
  { loading: ToolLoading }
);

const toolComponents: Record<string, React.ComponentType> = {
  // ... существующие инструменты
  'my-new-tool': MyNewTool,
};
```

**Готово!** Инструмент автоматически появится в каталоге на главной странице и будет доступен по адресу `/tools/analysis/my-new-tool`.

---

## ✅ Стратегия тестирования

Мы придерживаемся стратегии **"Colocation & Lightweight Unit Tests"**.

### 1. Где писать тесты

Тесты располагаются **рядом с тестируемым файлом**.

- `lib/modules/logic.ts` -> `lib/modules/logic.test.ts`

### 2. Что тестировать

В первую очередь покрываем тестами **Core** и **Modules** (чистую бизнес-логику и алгоритмы):

- Парсеры файлов (например, Godot Scene).
- Алгоритмы обработки графики (Flood Fill, Erosion).
- Утилиты фильтрации и валидации файлов.

UI-компоненты тестируются только если они содержат сложную изолированную логику.

### 3. Pipeline и Pre-commit

Мы используем **Vitest** в режиме `related`.

- При коммите (`pre-commit hook`) запускаются тесты **только для измененных файлов**.
- Это гарантирует, что проверка проходит мгновенно и не замедляет разработку.

---

## 🤖 Developer Experience (DX)

### Генерация контекста для ИИ

Мы используем подход **AI-Native Development**. В проекте есть скрипт `scripts/generate-context.ts`.

- **Output:** Генерирует папку `.context/` в корне проекта с файлом `project.txt`.
- **Логика:** Использует те же правила фильтрации файлов (ignore patterns), что и инструмент "Project to Context".
- **Автоматизация:** В режиме `npm run dev` работает в фоне (watch mode), обновляя контекст при каждом сохранении файла.

---

## 🖌 Стилизация

Проект использует **Tailwind CSS v4**.

- **Глобальные стили:** `app/globals.css`.
- **Линтинг:** Используется `stylelint` для CSS файлов и `eslint-plugin-tailwindcss` для автоматической сортировки классов в JSX.
- **Темы:** Поддерживается темная и светлая тема (системная или переключаемая). Используйте префикс `dark:` для стилизации темной темы.

## 🌐 Среда выполнения и совместимость

Проект придерживается стратегии **Web Baseline**.

### Целевая аудитория

Инструменты предназначены для технических специалистов (разработчики, дизайнеры), использующих современные браузеры.

### Конфигурация Browserslist

Мы используем запрос `baseline newly available` (доступен с Browserslist 4.26+).
Это гарантирует поддержку всех современных веб-стандартов (ES2024+, CSS Nesting, :has(), и т.д.), которые поддерживаются последними версиями основных движков (Blink, Gecko, WebKit).

Это позволяет:

1.  **Отказаться от тяжелых полифилов** для нативных методов (`toSorted`, `with`, `Array.fromAsync`).
2.  **Использовать современный CSS** без лишних префиксов.
3.  **Повысить производительность** за счет нативных API.

Поддержка устаревших браузеров (Legacy) не предусмотрена.
