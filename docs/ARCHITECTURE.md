# Архитектура проекта

Проект построен на **Next.js 16 (App Router)** с использованием модульной архитектуры. Мы придерживаемся принципов, близких к **FSD (Feature-Sliced Design)**, адаптированных для специфики портфолио-приложения.

## ⚛️ Ядро (The Core Kernel)

Ядро структурировано по **технологическим доменам**.

Это решает проблему "загрязнения контекста", когда серверные утилиты случайно попадают в клиентский бандл, или React-хуки импортируются в чистые вычислительные модули.

### Структура `core/`:

1.  **`core/primitives/`** (Universal)
    - **Что это:** Чистые функции на TypeScript. Математика, обработка строк, цветовые модели.
    - **Зависимости:** 0. Никаких фреймворков, никакого DOM.
    - **Где использовать:** Везде (Frontend, Backend, Tests, Workers).

2.  **`core/browser/`** (Client Only)
    - **Что это:** "Сахар" и адаптеры над Web API (DOM, Canvas, LocalStorage, File API, Navigator).
    - **Где использовать:** UI Components, Features.
    - **Запрещено:** Server Actions, Node Scripts.

3.  **`core/react/`** (Client Only)
    - **Что это:** Утилиты, специфичные для экосистемы React (Hooks, Contexts, HOCs).
    - **Где использовать:** UI Components, Features.
    - **Запрещено:** В чистых бизнес-модулях (`lib/`), на сервере.

4.  **`core/node/`** (Server Only)
    - **Что это:** Безопасные обертки над Node.js API (FileSystem, Streams, Child Process).
    - **Где использовать:** Scripts, Server Actions, API Routes.
    - **Строго запрещено:** В любом клиентском коде (UI, Features). Линтер заблокирует сборку при нарушении.

5.  **`core/next/`** (Framework Specific)
    - **Что это:** Конфигурации и утилиты Next.js (Fonts, Navigation, Metadata).
    - **Где использовать:** App Layer, UI Components (Link).

6.  **`core/tailwind/`** (Styling)
    - **Что это:** Утилиты для работы с CSS-классами (`cn`, `cva`).
    - **Где использовать:** UI Components.

---

## 🏗 Принципы слоев (Boundaries)

Мы используем жесткие правила линтера (`eslint-plugin-boundaries`) для предотвращения запутывания зависимостей.

### 1. Feature Layer (`features/`)

- **Назначение:** Полноценные инструменты. Сборка компонентов и логики в готовое решение.
- **Структура**: Файлы компонентов, лежащие в корне или подпапках.
- **Правила:**
  - Инструменты изолированы друг от друга.
  - Собирают UI из `ui/` и логику из `lib/`.
  - Могут использовать `core/react/` и `core/browser/`.
  - **Policy: UI Isolation:** В этом слое ЗАПРЕЩЕНО использование базовых HTML-тегов для верстки. Вся верстка должна идти через компоненты `ui/layout/` (Box, Stack, Group).

### 2. Entity Subsystems (`features/_*/`)

- **Назначение:** Подсистемы инструментов (локальные сущности).
- **Структура:** Группируются по доменам в подпапках с префиксом `_` (`features/_hardware/`, `features/_video/`).
- **Правила:** Может использовать `ui/` и `lib/`.

### 3. UI Library (`ui/`)

- **Назначение:** Внутренняя дизайн-система ("глупые" компоненты).
- **Структура:**
  - `ui/primitive/` — Атомы (Icon, Badge, Typography).
  - `ui/input/` — Элементы ввода (Button, Slider).
  - `ui/layout/` — Примитивы верстки (Stack, Group, Box, Modal).
  - `ui/canvas/` — Специализированные компоненты для работы с Canvas.
- **Правила:**
  - Здесь живут ВСЕ базовые HTML-теги и стили Tailwind. Компоненты этого слоя — единственные владельцы `<div>`, `<span>` и т.д.
  - Компоненты агностичны к бизнесу.
  - **Запрещено:** Импортировать бизнес-логику (`lib/`) или серверный код (`core/node/`).
  - Разрешено использовать `core/react/`, `core/browser/`, `core/tailwind/`.
  - Зависимости: Только `Radix UI`, `Tailwind` и утилиты из `core/`.

### 4. Domain Logic (`lib/`)

- **Назначение:** Основная бизнес-логика, сгруппированная по доменам (Graphics, FileSystem, Video).
- **Зависимости:** Могут использовать `core/primitives/`. Использование `core/browser/` (например, Canvas) допустимо, но должно быть изолировано.

### 5. App Registry Layer (`app/registry/`)

- **Назначение:** Связующий узел (Dynamic Imports, Inventory).
- **Правила:** Единственное место, знающее обо всех инструментах сразу.

---

## 🧠 Принципы проектирования (Design Principles)

### 1. Abstraction over Extraction

Мы отдаем предпочтение созданию универсальных абстракций перед простым дроблением кода.

- ✅ **Abstraction (Хорошо):** Выделить повторяющийся паттерн поведения в хук или класс, который ничего не знает о бизнес-контексте вызывающего.
  - _Пример:_ Вместо `handleWorkerMessage` внутри компонента, мы создаем `useWorker`, который инкапсулирует весь жизненный цикл Web Worker.

### 2. Colocation (Локализация)

Храните вещи там, где они используются.

- Стили, тесты, типы и вспомогательные функции компонента должны лежать рядом с ним (в той же папке или даже в том же файле).

### 3. Стратегия декомпозиции (Decomposition Strategy)

Процесс рефакторинга и выделения компонентов следует строгому направленному графу (Dependency Flow). Мы разделяем "умные" (Smart) и "глупые" (Dumb) компоненты.

**Поток данных:**
`Feature Implementation` → `Domain Component` → (`Shared UI` + `Business Logic`)

1.  **Feature Implementation (`features/[ToolName].tsx`):**
    Исходная точка входа. Может содержать смешанную логику и верстку до момента рефакторинга.
2.  **Domain Component (`features/_[domain]/`):**
    _Слой "Сущности" или Smart Component_.
    - **Роль:** Контроллер. Связывает данные (Store/Logic) с отображением.
    - **Импортирует:** `ui/` и `lib/`.
3.  **Atomic Elements (Результат декомпозиции):**
    - **A. Shared UI (`ui/`):** _Presentational Component_. Отвечает только за внешний вид и интерактивность. Полностью агностичен к данным.
    - **B. Business Logic (`lib/`):** Отвечают за обработку данных. Запрещен JSX/React.

---

## 🛡 Качество кода и TypeScript

Проект использует стратегию **Maximum Strictness**.

- **Paranoia Mode:** Включены флаги `noUncheckedIndexedAccess`, `noImplicitOverride` и др.
- **Next.js Conflict:** Папка `.next/dev` исключена из проверки типов из-за несоответствия строгости.

---

## 📚 Принципы документации

Мы применяем принцип **DRY (Don't Repeat Yourself)** к документации.

1.  **Лаконичность:** Максимум смысла, минимум текста.
2.  **Single Source of Truth:** Архитектура описана здесь, правила разработки — в `CONTRIBUTING.md`.
3.  **Code as Documentation:** Приоритет отдается понятным типам и именам, а не комментариям.
4.  **Positive Framing:** Правила формулируются как руководства («Делайте так»), указывая на предпочтительные обобщённые идеи, а не перечисляя запреты.

---

## 🧩 Модульная система инструментов

Добавление инструмента стандартизировано:

1.  **Конфигурация:** Метаданные в `app/registry/specifications.ts`.
2.  **Инвентарь:** Добавление в список в `app/registry/inventory.ts`.
3.  **Реализация:** Компонент в `features/[ToolName].tsx`.
4.  **Регистрация:** Lazy-loading через `dynamic import` в `app/registry/tool-loader.tsx`.

---

## ✅ Стратегия тестирования

Стратегия **"Colocation & Lightweight Unit Tests"**.

- **Где:** Тесты располагаются рядом с файлом (`logic.test.ts`).
- **Что:** В приоритете Core и Modules (чистая логика). UI тестируется редко.

---

## 🤖 Developer Experience (DX)

- **AI Context:** Скрипт `scripts/generate-context.ts` генерирует `project.txt` в папке `.context/` для удобной работы с LLM. Работает в watch-режиме при `npm run dev`.

---

## 🖌 Стилизация и Среда

- **Tailwind CSS v4:** Используется для всех стилей. Поддержка темной темы (`dark:`).
- **Browserslist:** `baseline newly available` (современные браузеры, без лишних полифилов).
