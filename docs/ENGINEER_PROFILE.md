# Портрет разработчика и Инструкции для LLM

Этот документ описывает компетенции, предпочтения и "слепые зоны" автора кода.
**LLM должна использовать этот профиль для калибровки своих ответов и предложений.**

## 👤 Роль: Архитектор и Владелец Логики

Разработчик фокусируется на **Data Flow, Type Safety, Модульности и Бизнес-логике**.

### ✅ Компетенции (Где разработчик эксперт)

- **Архитектура:** FSD (Feature-Sliced Design), Clean Architecture, Separation of Concerns.
- **TypeScript:** Режим "Paranoia Mode". Разработчик требует строгой типизации (`noUncheckedIndexedAccess`), ненавидит `any`, любит дженерики и type guards.
- **React Logic:** Хуки, жизненный цикл, мемоизация (`useMemo`, `useCallback`), композиция компонентов.
- **Алгоритмы:** Обработка данных, пайплайны, Web Workers.

> **Инструкция для LLM:** В этих областях предлагайте улучшения только если видите явную архитектурную ошибку или возможность значительной оптимизации. Не объясняйте базовые концепции React/TS.

---

### 🔻 Делегированные области (Где LLM должна быть смелее)

Разработчик признает ограниченность компетенций в этих областях и **полностью доверяет решениям LLM**.

#### 1. Tailwind CSS и Верстка

- **Статус:** "Слепое доверие генераторам". Разработчик проверяет только визуальный результат.
- **Проблема:** Код часто содержит конфликтующие классы, лишние отступы, "магические числа" и хаос в z-index.
- **Инструкция для LLM:**
  - **Действуйте авторитарно.** Если видите `p-4 p-2`, молча исправляйте на `p-2`.
  - **Внедряйте Best Practices.** Используйте `flex/grid` грамотно, заменяйте margin-хаки на `gap`.
  - **Не спрашивайте разрешения** на рефакторинг CSS-классов, если это делает код чище.
  - Используйте утилиту `cn()` и `cva()` по умолчанию.

#### 2. Конфигурация сборщиков (Vite, Next.js internals)

- **Статус:** "Работает — не трогай".
- **Инструкция для LLM:** Если конфигурация устарела или замедляет сборку — предлагайте готовые фиксы с объяснением _почему_ это важно.

#### 3. Boilerplate Code

- **Статус:** Раздражает.
- **Инструкция:** Генерируйте полный код для скучных частей (UI-кит, формы, моки данных), чтобы разработчик мог сфокусироваться на логике.

---

## ⚡ Стиль коммуникации

1.  **Лаконичность:** Минимум воды. Сразу к коду или архитектурному решению.
2.  **Без "поглаживаний":** Не нужно хвалить код перед тем, как указать на ошибку. Сразу указывайте на проблему.
3.  **Принцип "Исправляй, а не критикуй":** Вместо "тут можно было бы использовать cn", сразу дайте блок кода с `cn`.

## 🛠 Технические предпочтения

- **Библиотеки:** Radix UI, Tailwind CSS, Remeda (вместо Lodash/Ramda), Zod (валидация).
- **Паттерны:**
  - Functional Programming (где уместно).
  - Composition over Inheritance.
  - Colocation (тесты и стили рядом с компонентом).
