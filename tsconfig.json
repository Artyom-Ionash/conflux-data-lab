{
  "compilerOptions": {
    // Безопасный и современный стандарт для Node 18+
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    // Включаем проверку типов даже в JS файлах (максимальная строгость для смешанных проектов)
    "checkJs": true,
    "skipLibCheck": true,
    "strict": true,
    // --- MAXIMUM STRICTNESS ---
    // Гарантирует, что доступ по индексу (arr[0]) всегда возвращает Type | undefined
    "noUncheckedIndexedAccess": true,
    // Запрещает доступ к свойствам через точку, если они определены только через index signature
    "noPropertyAccessFromIndexSignature": true,
    // Требует ключевое слово 'override' при переопределении методов классов
    "noImplicitOverride": true,
    // Различает опциональные поля (?) и поля, которые могут быть undefined
    "exactOptionalPropertyTypes": true,
    // --- CODE QUALITY ---
    // Ошибка, если функция не возвращает значение во всех ветках кода
    "noImplicitReturns": true,
    // Ошибка при проваливании в switch-case без break/return
    "noFallthroughCasesInSwitch": true,
    // Ошибка при наличии недостижимого кода
    "allowUnreachableCode": false,
    // Ошибка при неиспользуемых лейблах
    "allowUnusedLabels": false,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "tsBuildInfoFile": "node_modules/.cache/tsbuildinfo",
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    },
    "typeRoots": ["./lib/types", "./node_modules/@types"]
  },
  "include": [
    "next-env.d.ts",
    "core/typescript/declarations/**/*.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "**/*.mts",
    ".next/dev/types/**/*.ts",
    "vitest.d.ts",
    "vitest.setup.ts"
  ],
  "exclude": ["node_modules", ".next/dev"]
}
