# 📉 Technical Debt & Roadmap

Этот документ отслеживает известные технические компромиссы и планы по развитию проекта.

## 🟡 Средний приоритет (DX & Stability)

### 1. Отсутствие автоматических тестов

**Проблема:** Критическая логика обработки данных (парсеры, калькуляторы таймингов видео) не покрыта тестами.
**Риски:** Рефакторинг может незаметно сломать логику инструментов.
**План действий:**

- [ ] Настроить **Vitest** (он быстрее и лучше совместим с Vite/Next экосистемой, чем Jest).
- [ ] Написать Unit-тесты для `lib/domain/` (например, `texture-standards.ts`).
- [ ] Написать тесты для `ProjectToContext.tsx` (парсинг файлов).

---

## 🟢 Низкий приоритет (Optimizations & Features)

### 1. Валидация имен классов Tailwind

**Проблема:** Мы перешли на Prettier для сортировки классов, но Prettier (в отличие от удаленного `eslint-plugin-tailwindcss`) **не проверяет** существование классов. Можно написать `bg-red-999` (несуществующий цвет), и ошибки не будет.
**План действий:**

- [ ] Дождаться выхода стабильного линтера для Tailwind v4.
- [ ] Вернуть валидацию классов в CI/CD пайплайн.

### 2. Производительность тяжелых вычислений

**Проблема:** Обработка видео (`VideoFrameExtractor`) и изображений (`MonochromeRemover`) происходит в основном потоке (Main Thread).
**Последствия:** При обработке больших файлов интерфейс может "подвисать".
**План действий:**

- [ ] Вынести пиксельные манипуляции в **Web Workers**.
- [ ] Использовать `OffscreenCanvas` для рендеринга без блокировки UI.

### 3. Синтаксис CSS и Stylelint

**Проблема:** Stylelint требует настройки `ignoreAtRules` и `import-notation`, чтобы понимать директивы Tailwind v4. Это временное решение в `stylelint.config.mjs`.
**План действий:**

- [ ] Перейти на официальный конфиг Stylelint для Tailwind v4, когда он станет стабильным и убрать ручные исключения.
