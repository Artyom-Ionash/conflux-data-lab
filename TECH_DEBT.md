# 📉 Technical Debt & Roadmap

Этот документ отслеживает известные технические компромиссы и планы по развитию проекта.

## 🟡 Средний приоритет (DX & Stability)

### 1. Отсутствие автоматических тестов

**Проблема:** Критическая логика обработки данных (парсеры Godot, алгоритмы заливки) не покрыта тестами.
**Риски:** Рефакторинг может незаметно сломать логику инструментов.
**План действий:**

- [ ] Настроить **Vitest** (он быстрее и лучше совместим с Vite/Next экосистемой, чем Jest).
- [ ] Написать Unit-тесты для `lib/modules/file-system/godot-scene.ts` (парсинг сцен).
- [ ] Написать тесты для `lib/modules/graphics/processing/filters.ts` (алгоритмы обработки пикселей).

### 2. Систематизация слоев вёрстки (Layout & Z-Index)

**Проблема:** В компонентах (`Canvas`, `ToolLayout`, `Modal`) встречаются хардкодные значения `z-index` (например, `z-50`, `z-[100]`). Это создает риск "войн слоев" (z-index wars), когда выпадающие меню перекрываются модальными окнами или наоборот.
**План действий:**

- [ ] Провести аудит текущих `z-index` в проекте.
- [ ] Внедрить семантическую систему слоев в тему Tailwind (например, `z-modal`, `z-tooltip`, `z-overlay`), отказавшись от магических чисел.
- [ ] Четко разделить ответственность между глобальным Layout (`app/layout.tsx`) и локальными Layouts инструментов.

---

## 🟢 Низкий приоритет (Optimizations & Features)

### 1. Валидация имен классов Tailwind

**Проблема:** Prettier сортирует классы, но не валидирует их существование в Tailwind v4. Можно случайно написать несуществующий класс.
**План действий:**

- [ ] Дождаться выхода стабильного плагина `eslint-plugin-tailwindcss` (или аналога) с поддержкой v4.
- [ ] Вернуть валидацию классов в CI/CD пайплайн.

### 2. Расширение использования Web Workers

**Проблема:**

- `MonochromeBackgroundRemover` ✅ Использует Worker.
- `VideoFrameExtractor` ⚠️ Обработка видео и генерация GIF происходят в основном потоке, что может блокировать UI при работе с большими файлами.
  **План действий:**
- [ ] Перенести генерацию GIF (`gifshot` или альтернативу) в Web Worker.
- [ ] Исследовать возможность использования `OffscreenCanvas` для извлечения кадров из видео (API поддерживается не во всех браузерах, нужен фоллбэк).

### 3. Синтаксис CSS и Stylelint

**Проблема:** В `stylelint.config.mjs` используются временные исключения (`ignoreAtRules`) для поддержки директив Tailwind v4 (`@theme`, `@utility`).
**План действий:**

- [ ] Перейти на официальный рекомендуемый конфиг Stylelint для Tailwind v4, когда он станет стабильным, и убрать ручные исключения.
