#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 1. Глобальная проверка целостности типов (защита от поломки зависимых файлов)
# Если tsc найдет ошибку в ЛЮБОМ файле проекта, коммит будет заблокирован.
echo "🔍 Running Full Type Check..."
npm run type-check

# 2. Быстрая проверка стиля и тестов только для измененных файлов
echo "🎨 Running Lint-Staged..."
npx lint-staged